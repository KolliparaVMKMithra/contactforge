# Login credentials (required on Render)
AUTH_EMAIL=your_email@example.com
AUTH_PASSWORD=your_secure_password
AUTH_SECRET_KEY=generate_a_long_random_string

# Optional — Hunter keys are stored in the browser, not on the server
# HUNTER_API_KEY=
